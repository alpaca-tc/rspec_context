# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in rspec_context.gemspec
gemspec

gem 'pry'
gem 'parser'
gem 'guard'
gem 'guard-rubocop'
gem 'guard-rspec'
